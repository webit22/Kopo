package day0315;

import java.util.Scanner;
import java.util.StringTokenizer;

public class BJ {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		String strCnt = sc.nextLine();

		StringTokenizer st = new StringTokenizer(strCnt, " ");
		int cnt1 = Integer.parseInt(st.nextToken());
		int cnt2 = Integer.parseInt(st.nextToken());

		// 듣도못한사람
		String[][] strArr = new String[cnt1][cnt2];
		for (int i = 0; i < cnt1; i++) {
			for (int j = 0; j < cnt2; j++) {
				strArr[i][j] = sc.nextLine();
			}

			if (i == cnt1 - 1) {
				// 비교

			}
		}

	}

}
